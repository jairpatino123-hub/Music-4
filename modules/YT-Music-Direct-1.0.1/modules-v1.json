{
  "contractVersion": 3,
  "releaseTrack": "staging",
  "contentType": "music",
  "moduleVersion": "1.0.1",
  "moduleFamilyId": "synthetiq_ytmusic_direct",
  "moduleIdentity": "SP-MUS-3031-YTMUSIC-DIRECT",
  "moduleIdentityNumber": 3031,
  "config": {
    "runtime": {
      "entry": "index.js",
      "mode": "local"
    },
    "caps": {
      "homeMaxResults": 50,
      "maxResponseBytes": 10485760,
      "timeoutMs": 30000,
      "maxConcurrentRequests": 3
    }
  },
  "configuration": {
    "fields": [
      {
        "key": "youtubeMusicApiKey",
        "type": "secret",
        "title": "YouTube Music InnerTube API Key (optional)",
        "required": false,
        "description": "Optional InnerTube key if the runtime requires one. No key is embedded in the package."
      }
    ]
  },
  "source": {
    "provider": "YouTube Music",
    "api": "https://music.youtube.com/youtubei/v1/search",
    "audioOnly": true,
    "playbackNote": "Search and metadata are supported. The connector does not fabricate or expose raw YouTube media URLs; playback is returned as the canonical YouTube Music route."
  }
}
